#include <stdio.h>
int absolute(int,int);
int compare (int,int);
int absolute (int num)
{
	int num1
	if(num < 0)
	{
		num = num*(-1);
		return num;
	}
	
}
int compare (absolute(num1_),absolute(num2_))
{
	if(absolute(num1_) < absolute(num2_))
	{
		return num2;
	}
	else if(absolute(num2_)<absolute(num1_))
	{
		return num1;
	}
}
int main (void)
{
	printf("수를 입력하시오.\n");
	scanf("%d %d",&num1,&num2); 
	num=absolute()
	printf("절댓값이 큰 값=%d",compare(num1,num2));
}

